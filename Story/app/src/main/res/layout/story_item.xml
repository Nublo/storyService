<?xml version="1.0" encoding="utf-8"?>

<com.andexert.library.RippleView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:ripple="http://schemas.android.com/apk/res-auto"
    android:id="@+id/more"
    android:layout_width="match_parent"
    android:layout_height="@dimen/material_list_item_height"
    ripple:rv_color="@color/font_selected_ripple"
    ripple:rv_type="rectangle">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/material_list_item_height"
        android:orientation="horizontal"
        android:paddingTop="@dimen/material_list_item_padding">

        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginLeft="@dimen/material_list_item_padding"
            android:layout_marginRight="@dimen/material_list_item_padding"
            android:background="@drawable/favorite_icon" />

        <TextView
            android:id="@+id/tv_story"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="@dimen/material_list_item_padding"
            android:maxLines="2"
            android:textSize="16sp"
            android:textColor="@color/primary_text" />

    </LinearLayout>

</com.andexert.library.RippleView>